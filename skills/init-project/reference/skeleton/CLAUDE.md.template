# Project: <项目名>数据审核报告

## Overview
临床试验数据审核报告项目。通过 Python + pandas 处理 EDC 导出的 Excel 数据，生成 .docx/.xlsx 报表。

## Directory Structure
```
├── 01 rawdata/          # 原始数据（不入 Git）
├── 02 metadata/         # EDC 元数据 Excel + build-metadata、init-project 生成的 JSON
├── 03 output/           # 生成的报表（不入 Git）
├── 04 scripts/            # 分析代码（新脚本默认平铺；已有章节目录保持不变）
├── utils/              # 公共工具函数
│   ├── loaders.py      # 数据读取层（load_sheet / system_cols）
│   ├── output_docx.py  # docx 三线表输出
│   ├── output_xlsx.py  # xlsx 清单输出
│   └── output_format.py # 报表输出聚合入口（re-export docx/xlsx）
├── .gitattributes      # Git 行尾与二进制规则
├── .gitignore          # Git 忽略规则
├── config.py           # 路径配置加载器（从 config.yaml 读取）
├── config.yaml         # 数据路径配置
└── requirements.txt    # Python 依赖
```

## Permissions
- `04 scripts/`、`utils/`、`config.py`、`config.yaml`：Claude Code 可编辑
- `01 rawdata/`、`03 output/`：不在 Git 中

## Conventions

编码规范（变量前缀、列名集中管理、八步操作模型、脚本模板等）详见 `/write-script` skill 的 `SKILL.md`。以下为跨 skill 的通用约定：

<!-- EDC_TYPE_HEADER_START -->
<!-- init-project Step 2b：按 Step 1 选定的 EDC 类型替换下方约定行， -->
<!-- 三类取值见 skills/write-script/reference/header-structure.md「写回 CLAUDE.md 的格式」节。 -->
- 表头结构：`header=0`（单行字段标签，编码字段含 `(fieldOID)` 缩写后缀；无 skiprows）
<!-- EDC_TYPE_HEADER_END -->
- 报表函数来自 `utils/output_format.py`
- 数据读取函数来自 `utils/loaders.py`（`load_sheet` / `system_cols`）
- 生成文件路径由 `config.yaml` 的 `output_path` 控制（`config.py` 自动解析为绝对路径）
- 虚拟环境位于 `.venv/`，安装依赖：`pip install -r requirements.txt`

## 强制约束

1. **改脚本前先加载 write-script skill**：编写/修改 `04 scripts/` 下 Python 脚本，必须先触发 `/write-script` 按其流程执行（仅单行拼写/注释修正可跳过）。
2. **查数据形状用 query_metadata.py，严禁直接读 `01 rawdata/`**：确认字段名/编码表/列名先查元数据（write-script Step 2 走 metadata-explorer）；raw 读取已被全局 hook（Bash 分段判定 + Grep 纳管 + 裸目录/任意后缀拦截 + 注释伪造 nrows 无效）与权限（Read/Grep deny raw）硬拦。`03 output/` **允许**读取以便核对脚本产出是否正确。兜底——穷尽元数据仍不够时，才可 `nrows≤2` 受控读（含表头≤3 行），仅确认字段/取样。
3. **改脚本后必须验证**：`04 scripts/` 脚本写/改后必须实跑（数据不可用时做语法检查）确认通过，才提醒复核；同一错误修 2 次仍失败则停下、将完整报错交用户定夺。

## Agent 清单

自定义 Agent 随 ClinSkills Plugin 安装，通过 Agent 工具的 `subagent_type` 参数调用（需在插件已启用时）：

| Agent | 模型 | 用途 | 典型调用场景 |
|---|---|---|---|
| `metadata-explorer` | 继承主会话 | 渐进式查询 EDC 元数据（search→fields→codelist），返回结构化结果 | write-script Step 2 自动调用；也可手动 spawn 探索数据形状 |
| `python-reviewer` | 继承主会话 | Python 脚本编码规范合规审查，对照 review-checklist.md 返回分级违规列表 | write-script Step 5 规范审查；review-changes 中 Python 维度 1/7 深度检查 |
