# Jupytext: pair .ipynb with .py (percent format)
formats = ["ipynb", "py:percent"]

# Preserve notebook-level metadata
notebook_metadata_filter = "-all"

# Optionally strip outputs on write
# [formats.ipynb]
# suffix = ".ipynb"
